<%= render "shared/header" %>
<div>
  <p><%= @trip.title %></p>
  <p>開始日：<%= @trip.start_date %></p>
  <p>終了日：<%= @trip.end_date %></p>
  <p><%= @trip.memo %></p>
    <% @trip.users.each do |user| %>
      <p><%= user.name %></p>
    <% end %>
</div>